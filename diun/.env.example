# Diun -> Telegram, notify-only. Same bot as every other alert in this project (see
# /srv/compose/scrutiny/.env for where the credentials already live - reuse them, don't duplicate).
DIUN_NOTIF_TELEGRAM_TOKEN=
DIUN_NOTIF_TELEGRAM_CHATIDS=
